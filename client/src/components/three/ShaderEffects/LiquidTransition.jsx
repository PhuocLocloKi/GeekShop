import React from 'react';

// Liquid Wave transition placeholder for routing transitions if needed
export const LiquidTransition = ({ children }) => {
  return <div className="liquid-transition-wrapper">{children}</div>;
};

export default LiquidTransition;
